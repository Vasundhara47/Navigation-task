import { themeTypes } from "./types";
import styled from "styled-components";

export const theme: themeTypes = {

    colors: {
        primary: "#0A1828",
        secondary: "#BFA181",
        terniry: "#FFFF"
    }
}